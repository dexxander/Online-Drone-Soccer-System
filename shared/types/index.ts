/**
 * PLACEHOLDER: Shared Data Models
 * 
 * WHAT THIS FILE CHANGES:
 * Acts as the single source of truth for TypeScript types across BOTH the React frontend and the Express backend.
 * 
 * TODO FOR THE TEAM:
 * 1. Move all interfaces from `src/lib/types.ts` into this file.
 * 2. Update frontend and backend imports to point to this `shared/types` directory.
 * 3. Add new models requested: 
 *    - `MatchEvent` (for logging goals/penalties with timestamps).
 *    - `Penalty` (Minor, Major, Technical).
 */
